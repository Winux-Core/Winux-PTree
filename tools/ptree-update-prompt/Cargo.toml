[package]
name = "ptree-update-prompt"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
eframe = { version = "0.27", default-features = false, features = ["default_fonts", "glow", "x11"] }

[workspace]
